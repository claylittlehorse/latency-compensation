return {
	ScalingBoundaries = {
		MIN_SIZE = Vector2.new(720, 720),
		MAX_SIZE = Vector2.new(1080, 1920)
	},

	LayoutTypes = {
		DESKTOP = "DESKTOP",
		GAMEPAD = "GAMEPAD"
	},

	ROOT_NAME = "GameUI"
}
